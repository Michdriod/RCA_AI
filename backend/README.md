# Backend
FastAPI service, session mgmt in Redis, 5-Whys engine via Pydantic AI.

Dirs:
- app/api: endpoint routers
- app/core: config, settings
- app/services: session & persistence abstractions
- app/models: pydantic schemas
- app/ai: prompt templates & AI logic
- tests: unit/integration tests
